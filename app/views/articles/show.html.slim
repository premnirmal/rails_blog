center
  h1 = @article.title
  h3 
    i class="icon-chevron-left"
    = " " + to_date(@article.created_at) + " "
    i class="icon-chevron-right"
p
  = markdown(@article.body)

- unless @article.tags.empty?
  p
    h4 Tags:&nbsp
    = @article.tag_names

br
hr
p
  = raw(disqus_thread)